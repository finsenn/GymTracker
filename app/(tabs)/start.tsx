import React from 'react';
import { View } from 'react-native';

// This screen is a placeholder. It will never be shown because the tab
// button navigates to the modal instead.
export default function StartScreenPlaceholder() {
  return <View />;
}
